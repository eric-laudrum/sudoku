<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Highlight Options -->
    <group
        android:id="@+id/highlight_mode_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/action_highlight_all_numbers"
            android:title="Highlight All Numbers"

            app:showAsAction="never" />

        <item
            android:id="@+id/action_highlight_active_cell_numbers"
            android:title="Highlight Active Number"
            android:checked="true"
            app:showAsAction="never" />

    </group>

    <item
        android:id="@+id/action_set_single_highlight_color"
        android:title="Set Single Highlight Color"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_change_background_color"
        android:title="Change Background Colour"
        app:showAsAction="never"/>

</menu>
